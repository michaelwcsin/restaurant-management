import React, {useContext} from "react"
import { useCart } from "./cartContext"


const Cart = () => {
    const cartContext = useContext(CartCon)
    const cartTotal = useCart.






    return (
        <div></div>
    )
}